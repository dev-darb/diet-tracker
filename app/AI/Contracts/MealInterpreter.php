<?php

namespace App\AI\Contracts;

/**
 * SCAFFOLD ONLY — implemented in Milestone 5 (brief §8.5, §4.3).
 *
 * Marks the future natural-language meal-entry capability: turn free text such
 * as "100g pasta and about half the chicken pack" into structured candidate
 * ingredients that the consumption service (M4) maps to pantry items.
 * Marker interface only; the method surface is fixed when M5 is built.
 */
interface MealInterpreter {}
